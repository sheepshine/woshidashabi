// quick sort
function  quickSort(arr, left, right) {
	// if(arr.length<=1){
 //        return arr;
 //    }
	// let left = []
	// let right = []
	// let key = arr.splice(0, 1)[0]
	// for (let i = 0; i < arr.length; i++) {
	// 	if (key > arr[i]) {
	// 		right.push(arr[i])
	// 	} else {
	// 		left.push(arr[i])
	// 	}
	// }

	// console.log(left, right)
	// return quickSort(right).concat([key], quickSort(left))
	if (left < right) {
		let pos = left - 1
		for (left i = left; i <= right; i++) {
			let pivot = arr[right]
			if (arr[i])
		}
	}

}

console.log(quickSort([2,5,6,7,0,4,2]))